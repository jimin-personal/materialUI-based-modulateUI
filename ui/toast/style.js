import { makeAppStyles } from '@/lib/styleHelper';

export const useToastContainerStyles = makeAppStyles((theme) => ({
  container: {
    background: 'unset',
    padding: '0px',
    margin: '0px',
    boxShadow: 'unset',
  },
}));
